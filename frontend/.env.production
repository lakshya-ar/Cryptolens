# Production build config, read automatically by `vite build`.
# These are public endpoint URLs (they end up in the client bundle regardless),
# so committing them is safe and makes deploys reproducible.
# MUST match render.yaml's frontend envVars — these are the live, working
# Singapore-region services. (The old -qayj / cryptolens-ws pair is dead.)
VITE_API_BASE=https://cryptolens-api-5umm.onrender.com/api
VITE_WS_URL=wss://cryptolens-ws-i1bj.onrender.com
