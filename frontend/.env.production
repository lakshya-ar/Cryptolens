# Production build config, read automatically by `vite build`.
# These values are now injected dynamically by Render Blueprint 
# (via the render.yaml fromService properties) during the build step.

# Fallback values just in case local testing requires them:
VITE_API_BASE=
VITE_WS_URL=