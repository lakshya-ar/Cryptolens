# Production build config, read automatically by `vite build`.
# These are public endpoint URLs (they end up in the client bundle regardless),
# so committing them is safe and makes deploys reproducible.
VITE_API_BASE=https://cryptolens-api-qayj.onrender.com/api
VITE_WS_URL=wss://cryptolens-ws.onrender.com
